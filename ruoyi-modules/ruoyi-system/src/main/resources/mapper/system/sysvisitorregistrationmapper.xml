<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.system.mapper.SysVisitorRegistrationMapper">

    <resultMap type="org.dromara.system.domain.vo.SysVisitorRegistrationVo" id="SysVisitorRegistrationVoResult">
        <result property="visitorId"        column="visitor_id"        />
        <result property="visitorName"      column="visitor_name"      />
        <result property="phoneNumber"    column="phone_number"    />
        <result property="visitPurpose"   column="visit_purpose"   />
        <result property="deptId"         column="dept_id"         />
        <result property="appointmentTime" column="appointment_time"/>
        <result property="actualArrivalTime" column="actual_arrival_time"/>
        <result property="actualDepartureTime" column="actual_departure_time"/>
        <result property="status"         column="status"         />
        <result property="remark"         column="remark"         />
        <result property="createTime"     column="create_time"     />
        <result property="deptName"       column="dept_name"       />
    </resultMap>

    <sql id="selectSysVisitorRegistrationVo">
        select v.visitor_id, v.visitor_name, v.phone_number, v.visit_purpose, v.dept_id, 
               v.appointment_time, v.actual_arrival_time, v.actual_departure_time, v.status, 
               v.remark, v.create_time, d.dept_name
        from sys_visitor_registration v
        left join sys_dept d on v.dept_id = d.dept_id
    </sql>

    <select id="selectVoPage" resultMap="SysVisitorRegistrationVoResult">
        <include refid="selectSysVisitorRegistrationVo"/>
        <where>
            <if test="ew != null and ew.sqlSegment != null">
                AND ${ew.sqlSegment}
            </if>
        </where>
        order by v.create_time desc
    </select>

</mapper>