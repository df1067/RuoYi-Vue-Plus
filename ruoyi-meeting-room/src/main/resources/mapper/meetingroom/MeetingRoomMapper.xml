<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.meetingroom.mapper.MeetingRoomMapper">

    <resultMap type="MeetingRoom" id="MeetingRoomResult">
        <id property="roomId" column="room_id"/>
        <result property="roomName" column="room_name"/>
        <result property="roomType" column="room_type"/>
        <result property="capacity" column="capacity"/>
        <result property="location" column="location"/>
        <result property="equipment" column="equipment"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createDept" column="create_dept"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectMeetingRoomVo">
        select room_id, room_name, room_type, capacity, location, equipment, status, del_flag, create_dept, create_by, create_time, update_by, update_time, remark from meeting_room
    </sql>

    <select id="selectMeetingRoomList" parameterType="MeetingRoomBo" resultMap="MeetingRoomResult">
        <include refid="selectMeetingRoomVo"/>
        <where>
            <if test="roomName != null and roomName != ''"> and room_name like concat('%', #{roomName}, '%')</if>
            <if test="roomType != null and roomType != ''"> and room_type = #{roomType}</if>
            <if test="status != null and status != ''"> and status = #{status}</if>
            <if test="delFlag != null and delFlag != ''"> and del_flag = #{delFlag}</if>
        </where>
        order by room_id
    </select>

    <select id="selectMeetingRoomById" parameterType="Long" resultMap="MeetingRoomResult">
        <include refid="selectMeetingRoomVo"/>
        where room_id = #{roomId}
    </select>

</mapper>